# Segregated-Wallet-Provider
Bitcoin Layer Two Sollution
# What is SWP?
SWP is a simple frictionless layer two sollution to the bitcoin scalability problem, it uses the same cryptography as the first layer the bitcoin blockchain.
